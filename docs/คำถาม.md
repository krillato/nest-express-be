- findAll, findOne, create, update, delete คืออะไร
- ถ้าจะ join ทำยังไง
- this. คืออะไร
- private,constructor คืออะไร
- this.nextId++ ?
- @Injectable() ยังไม่ค่อยเข้าใจเท่าไหร่
- ทำไม relative import ใน NestJS โปรเจกต์นี้ต้องมี `.js` ต่อท้าย ทั้งที่ไฟล์จริงเป็น `.ts` — ต่างจาก
   `express-backend-ts` ยังไง เพราะอะไร?
- 5. Checklist ทวนความเข้าใจ ช่วยเฉลยทั้งหมด


คำถาม day 12
- @Entity() คืออะไร
- @PrimaryGeneratedColumn() คืออะไร
- @Column() คืออะไร
- @Column('decimal') คืออะไร
- @Column({ nullable: true }) คืออะไร
- @InjectRepository() คืออะไร
- private readonly repo: Repository<Product> คืออะไร
- ทำไมต้อง     async findOne
- TypeOrmModule พวก ORM มีอะไรบ้างคืออะไรช่วยอะไร